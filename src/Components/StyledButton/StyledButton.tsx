import styled from "@emotion/styled/macro";

const StyledButton= () => {


    const StyledButton = styled("button")(() => ({
       
      }));
    
  

    return (
      <>
      <StyledButton>
        TEXTO 
      </StyledButton>
      </>
    )
  }
  
  export default StyledButton
  